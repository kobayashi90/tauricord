name: Tauri Flatpak Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-flatpak:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Flatpak
        run: |
          sudo apt-get update
          sudo apt-get install -y flatpak flatpak-builder curl

      - name: Add Flathub remote
        run: |
          flatpak remote-add --if-not-exists --user flathub https://flathub.org/repo/flathub.flatpakrepo

      - name: Install SDKs
        run: |
          flatpak install -y --user flathub org.freedesktop.Platform//22.08
          flatpak install -y --user flathub org.freedesktop.Sdk//22.08
          flatpak install -y --user flathub org.freedesktop.Sdk.Extension.rust-stable//22.08

      - name: Build Flatpak app
        run: |
          flatpak-builder --force-clean --user build-dir io.tauricord.yml

      - name: Bundle Flatpak (optional)
        run: |
          flatpak-builder --repo=repo --force-clean --user build-dir io.tauricord.yml
          flatpak build-bundle repo Tauricord.flatpak io.tauricord

      - name: Upload Flatpak bundle
        uses: actions/upload-artifact@v4
        with:
          name: Tauricord.flatpak
          path: Tauricord.flatpak
